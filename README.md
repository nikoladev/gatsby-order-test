# Instructions

Run `yarn develop` and go to `localhost:8000`. All numbers in the images should match the numbers in the text underneath the image.

Now run `yarn build && yarn serve` and go to `localhost:9000`. The numbers in the images and the numbers in the text don't match up anymore.
